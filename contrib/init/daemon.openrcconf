# /etc/conf.d/gulden_daemon: config file for /etc/init.d/gulden_daemon

# Config file location
#DAEMON_CONFIGFILE="/etc/gulden/gulden.conf"

# What directory to write pidfile to?  (created and owned by $DAEMON_USER)
#DAEMON_PIDDIR="/var/run/gulden_daemon"

# What filename to give the pidfile
#DAEMON_PIDFILE="${DAEMON_PIDDIR}/gulden_daemon.pid"

# Where to write data (be mindful that the blockchain is large)
#DAEMON_DATADIR="/var/lib/gulden_daemon"

# User and group to own process
#DAEMON_USER="gulden"
#DAEMON_GROUP="gulden"

# Path to executable
#DAEMON_BIN="/usr/bin/Gulden-daemon"

# Nice value to run under
#DAEMON_NICE=0

# Additional options (avoid -conf and -datadir, use flags above)
#DAEMON_OPTS=""

# The timeout in seconds OpenRC will wait for daemon to terminate
# after a SIGTERM has been raised.
# Note that this will be mapped as argument to start-stop-daemon's
# '--retry' option, which means you can specify a retry schedule
# here. For more information see man 8 start-stop-daemon.
DAEMON_SIGTERM_TIMEOUT=60
